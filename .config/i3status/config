general {
        colors = false
        interval = 1
}

order += "disk /"
order += "tztime local"

disk "/" {
        format = "%avail "
        prefix_type = "decimal"
}

tztime local {
        format = "%d.%m.%Y %H:%M:%S"
}

